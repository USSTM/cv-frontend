## Agent skills

### Issue tracker

Issues and PRDs are tracked in GitHub Issues for `USSTM/cv-frontend`. See `docs/agents/issue-tracker.md`.

### Triage labels

Use the default Matt Pocock skills triage label vocabulary. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context frontend repo: read root `CONTEXT.md` and `docs/adr/` when present; consult backend domain/API context when frontend behavior depends on server contracts. See `docs/agents/domain.md`.
